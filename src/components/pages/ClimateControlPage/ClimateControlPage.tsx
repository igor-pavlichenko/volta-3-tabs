type Props = {};

const ClimateControlPage = (props: Props) => {
  // climate control instead, and then log everything on each change caused by user
  return <div>ClimateControlPage</div>;
};

export default ClimateControlPage;
